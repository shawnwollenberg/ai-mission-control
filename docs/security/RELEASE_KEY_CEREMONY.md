# Superseded offline Mission Agent release-key ceremony

This procedure was retired before generating
`mission-agent-release-2026-01`. The approved design uses AWS KMS as described
in `AWS_KMS_RELEASE_AUTHORITY.md`.

Do not generate, recover, import, or back up an offline PEM for this key ID.
KMS key creation, signer-role configuration, trust introduction, signing,
activation, publication, and deployment remain separately authorized.
